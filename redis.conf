# المنفذ الذي يستمع عليه Redis
port 6379

# السماح بالاتصال من localhost فقط
bind 127.0.0.1

# تفعيل حماية كلمة المرور (اختياري)
# requirepass yourpassword

# الحد الأقصى لاستخدام الذاكرة (اختياري)
maxmemory 100mb
maxmemory-policy allkeys-lru